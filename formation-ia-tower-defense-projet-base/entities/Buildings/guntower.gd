extends Node2D

# Called when the node enters the scene tree for the first time.
func _ready():
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	var nodeTarget =get_node("shooter")
#	var classs = nodeTarget.get_class()
#	self.rotation_degrees = rad2deg(self.position.angle()) + delta
	
